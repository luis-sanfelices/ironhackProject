<! — views/partials/nav.ejs ->
<div class="nav-tabs-navigation">
    <div class="logo-nav-bar">
        IronHack Proyect
    </div>
    <div class="nav-tabs-wrapper">
        <ul class="nav nav-tabs" role="tablist">
            <li id="home-nav-bar" class="nav-item">
                <a class="nav-link" data-toggle="tab" onclick='window.location.href="/"' role="tab">Home</a>
            </li>
            <%
            if ( typeof(locals.isAuthenticated) != "undefined" && locals.isAuthenticated) {%>
                <li id="profiles-nav-bar" class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%-locals.user.username%></a>
                    <ul class="dropdown-menu ">
                        <div class="dropdown-header" data-toggle="tab"><%-locals.user.role%></div>
                        <a class="dropdown-item" href="/profiles/<%-locals.user.role%>">Profile</a>
                        <a class="dropdown-item" href="/logout">Logout</a>
                    </ul>
                </li>
            <%} else {%>
                <li id="login-nav-bar" class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="modal" data-target="#signin-modal">Sign in</a>
                </li>
                <li id="signup-family-nav-bar" class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="modal" data-target="#signup-family-modal">Sign up as Family</a>
                </li>
                <li id="signup-profesional-nav-bar" class="nav-item">
                        <a class="nav-link" role="tab" data-toggle="modal" data-target="#signup-professional-modal">Sign up as Professional</a>
                </li>
                <%}%>
        </ul>
    </div>
</div>
